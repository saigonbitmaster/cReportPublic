module.exports = app => {
  //uncomment below code to get notified
  /* 
  app.models.Email.send(
    {
      to: 'bitmaster.sg@gmail.com',
      from: 'creportoshelp@gmail.com',
      subject: 'api is just restarted',
      //if there is no html then text will be email body.
      text: 'api is just restarted',
      html: 'my <em> api is just restarted </em>',
    },
    function(err, email) {
      if (err) {
        console.log(err);
      } else console.log('email sent!');
    },
  ); */
};